{
  "name": "blackmagicwoman",
  "description": "blackmagicwoman app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "mocha --bail --colors --reporter spec --ui bdd test"
  },
  "dependencies": {
    "express": "4.9.0"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "mocha": "^2.0.1"
  }
}
