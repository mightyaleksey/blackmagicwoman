{
  "name": "blackmagicwoman",
  "description": "blackmagicwoman app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app",
    "test": "mocha --bail --colors --reporter spec --ui bdd test"
  },
  "dependencies": {
    "configs-overload": "^0.2.0",
    "express": "4.9.0"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "mocha": "^2.0.1"
  }
}
